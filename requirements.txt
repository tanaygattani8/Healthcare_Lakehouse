duckdb==1.1.3
databricks-sql-connector==3.4.0
streamlit==1.39.0
pandas==2.2.3
# unpinned: databricks-sql-connector constrains the range. We only need
# *some* pyarrow so pandas can read/write parquet.
pyarrow
pytest==8.3.3
ruff==0.7.4
