# Synthea output — never committed
synthea/output/
synthea/*.jar

# Secrets
.env
*.pem

# Python
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.venv/
venv/

# Databricks
.databricks/

# Local-only working documents (D27)
CLAUDE.md
docs/plans/
docs/specs/
docs/learning/

# Airflow runtime and credentials
orchestration/.env
orchestration/logs/
orchestration/config/
orchestration/plugins/

# The answer key. Holds every patient's real name and every real date, with
# the exact position of each in their note. Never commit it.
data/
