# Synthea output — never committed
synthea/output/
synthea/*.jar

# Secrets
.env
*.pem

# Python
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.venv/
venv/

# Databricks
.databricks/

# Local-only working documents. Deliberately not published: the plan and the
# spec are working instruments, and CLAUDE.md is addressed to a tool rather
# than to a reader. The published docs/ files stand on their own.
CLAUDE.md
docs/plans/
docs/specs/
